<!DOCTYPE html>
<html>
    <head>
        <title>Five-Dice Server</title>
        <link href="{{ app.request.basepath }}/css/main.css" rel="stylesheet" type="text/css" />
    </head>
    <body>
        <div class="content">
            {% block content %}{% endblock %}
        </div>
        <script>
            var routes = {
                create: '{{ path('create') }}',
                pending: '{{ path('pending') }}',
                join: '{{ path('join', {hash: '12345678'}) }}',
                state: '{{ path('get_state', {hash: '12345678'}) }}'
            }
        </script>
        <script type="text/javascript" src="{{ app.request.basepath }}/components/jquery/dist/jquery.js"></script>
        <script src="/js/app.js"></script>
        {% block scripts %}{% endblock %}
    </body>
</html>
